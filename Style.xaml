Some information in xaml file.

File contetnt

<Grid>
                    <Grid Visibility="{Binding Path=MyContract.MyFormResponse, Converter={StaticResource CollapsedIfNotNullConverter}}" 
                          VerticalAlignment="Center" 
                          HorizontalAlignment="Center">
                        <TextBlock Text="No signed contracts were found." FontSize="16" Foreground="Gray"/>
                    </Grid>
                    <Grid Visibility="{Binding Path=MyContract.MyFormResponse, Converter={StaticResource CollapsedIfNullConverter}}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Button Grid.Row="0" HorizontalAlignment="Right" Content="Print Contract" Click="print_Click"/>
                        <Border Grid.Row="1" Name="contractContainer" Background="White" />
                    </Grid>
                    
                </Grid>

Eng of file/